const frutas = ['maca', 'pera', 'uva', 'salada mista', 'piroca', 'rola', 'buceta', 'cu', 'filho da puta', 'puto', 'viado'];

for (let i = 0; i < frutas.length; i++) {
    const element = frutas[i];
    console.log(`Estamos na fruta de indice ${i} e a fruta é ${element}`);
}

console.log('O numero total de frutas do array é: ', frutas.length)


// for (let i = 0; i <= 10; i++) {
//     const par = i % 2 === 0 ? 'par' : 'imppar';
//     console.log(i, par);
// }
